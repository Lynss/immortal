package com.ly.immortal

import org.junit.Test
import org.junit.runner.RunWith
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.test.context.junit4.SpringRunner

@RunWith(SpringRunner::class)
@SpringBootTest
open class BaseTest{
    @Test
    fun gitStep() {
//        git status;
//        git add.;
//        git commit -m"";
//        git fetch origin master:remote;从远程主干上拉取master到本地创建remote分支或是直接合并本地分支
//        git merge remote;
//        git push -u origin master:master;将本地分支推送到远程的master上；
//        git push origin :master;将空的分支推送到远程master上，删除远程的master分支。
    }
}
